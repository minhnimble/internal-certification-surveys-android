<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/dlSurveys"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="end">

    <include
        layout="@layout/content_fragment_surveys"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nvDrawerContainer"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="@color/nero0_90a"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/navigation_drawer_header_surveys"
        app:itemTextColor="@color/white_50a"
        app:itemTextAppearance="@style/Widget.Template.TextView.MenuItem"
        app:layout_constraintBottom_toBottomOf="@+id/navHostContainer"
        app:layout_constraintEnd_toEndOf="@+id/navHostContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:menu="@menu/menu_drawer_surveys"
        app:theme="@style/NavigationViewStyle" />

</androidx.drawerlayout.widget.DrawerLayout>
